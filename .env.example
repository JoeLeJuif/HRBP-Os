# HRBP OS — Supabase env vars (injected at build time by build.js via esbuild --define)
# Set these in Vercel (Project → Settings → Environment Variables) and locally if testing.
# The publishable key is safe to ship to the browser; never put the service_role key here.

VITE_SUPABASE_URL=https://dhemuvqwqtkrfmzflghx.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_nflgWUSEYciWVyZUMhswuw_3LxsIDjE
